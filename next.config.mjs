/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.pexels.com','localhost','shrineyatra-backend.vercel.app'],
    },
  };
  
  export default nextConfig;
  